{
  "scripts": {
    "install-packages": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm --prefix backend run dev",
    "client": "npm --prefix frontend start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
